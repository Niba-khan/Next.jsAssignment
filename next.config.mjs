/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['w0.peakpx.com', 'cliply.co','www.icegif.com','i.pinimg.com', 'i0.wp.com'], 
    },
  };
  
  export default nextConfig; // Use export default instead of module.exports
  